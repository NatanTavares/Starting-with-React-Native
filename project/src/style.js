import { StyleSheet } from "react-native";

export default StyleSheet.create({
  largerText: {
    fontSize: 32,
    fontWeight: "bold",
    color: '#fff',
  },
  regularText: {
    fontSize: 18,
    color: '#fff',
  },
  smallText: {
    fontSize: 12,
    color: '#fff',
  },
});